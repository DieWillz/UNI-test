[pytest]
# Ground-truth (E-group): pytest-asyncio in auto mode so async tests
# are handled without explicit markers.
asyncio_mode = auto
pythonpath = . tests/operator
testpaths = tests
addopts = -p no:cacheprovider --tb=short --import-mode=importlib
timeout = 90
filterwarnings =
    ignore::SyntaxWarning
