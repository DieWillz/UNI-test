# uni/requirements.txt — теперь ссылается на единый constraints.txt (Директива §6 Q-09).
# Раньше здесь была отдельная копия диапозонов (расходилась с корнем, напр. torch).
# Все версии — в constraints.txt.
-r constraints.txt
