[pytest]
# 🤖 Ground-truth (E-группа): pytest-asyncio в auto-режиме, чтобы
# async-тесты (test_contracts_and_features) обслуживались без явных маркеров.
asyncio_mode = auto
pythonpath = .
testpaths = tests
addopts = -p no:cacheprovider
