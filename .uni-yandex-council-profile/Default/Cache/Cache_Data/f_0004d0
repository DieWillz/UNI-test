<content>/* =====================================================================
UNI CONSOLE v2.7 — MASTER STYLESHEET
Тёплый графит + янтарный акцент, компактные карточки участников
===================================================================== */
:root{
color-scheme:dark;
--bg:#0f0d0a; --bg-2:#14110d; --panel:#1b1712; --panel-2:#241e17; --panel-3:#2d251b;
--ink:#efe9df; --muted:#a89d8d; --faint:#6f6555;
--line:#362b1e; --line-soft:#262015;
--accent:#f6a11c; --accent-hi:#ffbc4a; --accent-ink:#1d1204;
--ok:#4ade80; --danger:#f87171; --warn:#fbbf24;
--browser:#6fc3ef; --api:#4ade80; --codex:#c4b5fd;
--radius-lg:16px; --radius:12px; --r-sm:9px; --r-xs:6px;
--shadow-sm:0 1px 2px rgba(0,0,0,.4);
--shadow-md:0 8px 24px -8px rgba(0,0,0,.55);
--shadow-lg:0 24px 70px -16px rgba(0,0,0,.7);
--ease:cubic-bezier(.2,.7,.3,1);
--display:"Space Grotesk","Inter",system-ui,sans-serif;
--sans:"Inter",system-ui,-apple-system,"Segoe UI",Roboto,sans-serif;
--mono:"JetBrains Mono",ui-monospace,SFMono-Regular,Menlo,Consolas,monospace;
}
*{box-sizing:border-box;margin:0;padding:0}
html,body{height:100%}
body{
background:
radial-gradient(1100px 560px at 10% -12%, color-mix(in srgb,var(--accent) 8%,transparent), transparent 60%),
radial-gradient(900px 500px at 108% 112%, color-mix(in srgb,var(--browser) 5%,transparent), transparent 55%),
var(--bg);
color:var(--ink);font-family:var(--sans);font-size:13px;line-height:1.5;
display:flex;flex-direction:column;overflow:hidden;-webkit-font-smoothing:antialiased;
}
button,input,textarea,select{font-family:inherit;font-size:inherit;color:inherit}
::selection{background:color-mix(in srgb,var(--accent) 32%,transparent);color:#fff}
:focus-visible{outline:2px solid color-mix(in srgb,var(--accent) 70%,transparent);outline-offset:2px;border-radius:4px}
.empty{color:var(--faint);font-style:italic;font-size:12px}
.hint{color:var(--faint);font-size:11px}
.spacer{flex:1}

/* ---------- скроллбары ---------- */
*{scrollbar-width:thin;scrollbar-color:color-mix(in srgb,var(--faint) 45%,transparent) transparent}
*::-webkit-scrollbar{width:10px;height:10px}
*::-webkit-scrollbar-track{background:transparent}
*::-webkit-scrollbar-thumb{background:color-mix(in srgb,var(--faint) 40%,transparent);border-radius:10px;border:2.5px solid var(--bg)}
*::-webkit-scrollbar-thumb:hover{background:color-mix(in srgb,var(--faint) 65%,transparent)}
*::-webkit-scrollbar-thumb:active{background:var(--accent)}
*::-webkit-scrollbar-corner{background:transparent}
/* ---------- topbar ---------- */
.topbar{display:flex;align-items:center;gap:10px;padding:10px 16px;background:var(--bg-2);
border-bottom:1px solid var(--line-soft);flex-wrap:wrap;position:relative;z-index:10}
.brand{display:flex;align-items:center;gap:10px;font-family:var(--display);font-weight:700;
letter-spacing:.02em;white-space:nowrap;font-size:15px}
.brand .mark{position:relative;width:10px;height:10px;border-radius:50%;
background:radial-gradient(circle at 35% 30%,var(--accent-hi),var(--accent));
box-shadow:0 0 0 3px color-mix(in srgb,var(--accent) 16%,transparent),0 0 12px color-mix(in srgb,var(--accent) 75%,transparent)}
.brand .mark::after{content:"";position:absolute;inset:-6px;border-radius:50%;
border:1px solid color-mix(in srgb,var(--accent) 45%,transparent);animation:radar 2.4s var(--ease) infinite}
@keyframes radar{0%{transform:scale(.4);opacity:.9}100%{transform:scale(2.2);opacity:0}}
.brand small{color:var(--faint);font-weight:500;font-size:11px;font-family:var(--sans);letter-spacing:0}
.pill{display:inline-flex;align-items:center;gap:7px;padding:5px 11px;border:1px solid var(--line);
border-radius:999px;font-size:11px;color:var(--muted);background:var(--panel);white-space:nowrap;font-family:var(--mono)}
.pill b{color:var(--ink);font-weight:600}
.pill .led{width:6px;height:6px;border-radius:50%;background:var(--ok);box-shadow:0 0 7px color-mix(in srgb,var(--ok) 80%,transparent);flex:none}
.pill .led.amber{background:var(--warn);box-shadow:0 0 7px color-mix(in srgb,var(--warn) 80%,transparent)}
.gate-chip{display:inline-flex;align-items:center;gap:7px;padding:5px 11px;
border:1px solid color-mix(in srgb,var(--ok) 30%,var(--line));border-radius:999px;font-size:10.5px;color:var(--ok);
background:color-mix(in srgb,var(--ok) 7%,transparent);cursor:default;white-space:nowrap}
.gate-chip::before{content:"";width:6px;height:6px;border-radius:50%;background:var(--ok);box-shadow:0 0 7px color-mix(in srgb,var(--ok) 80%,transparent)}
.btn{border:1px solid var(--line);background:linear-gradient(180deg,var(--panel-2),var(--panel));
color:var(--ink);padding:8px 14px;border-radius:var(--r-sm);cursor:pointer;font-weight:500;
transition:.16s var(--ease);white-space:nowrap;box-shadow:var(--shadow-sm);font-size:12.5px}
.btn:hover{background:var(--panel-3);border-color:color-mix(in srgb,var(--faint) 60%,transparent);transform:translateY(-1px)}
.btn:active{transform:translateY(0)}
.btn.primary{background:linear-gradient(180deg,var(--accent-hi),var(--accent));color:var(--accent-ink);
border-color:transparent;font-weight:700;box-shadow:0 6px 20px -6px color-mix(in srgb,var(--accent) 60%,transparent)}
.btn.primary:hover{filter:brightness(1.07);transform:translateY(-1px)}
.btn.ghost-amber{background:color-mix(in srgb,var(--accent) 8%,transparent);border-color:color-mix(in srgb,var(--accent) 42%,transparent);color:var(--accent)}
.btn.ghost-amber:hover{background:color-mix(in srgb,var(--accent) 15%,transparent);border-color:var(--accent)}
.btn.sm{padding:5.5px 11px;font-size:11.5px}
.btn.on{border-color:var(--accent);color:var(--accent);background:color-mix(in srgb,var(--accent) 10%,transparent)}
.btn:disabled{opacity:.45;cursor:not-allowed;transform:none;box-shadow:none}
.link-btn{background:none;border:none;color:var(--faint);font-size:9.5px;cursor:pointer;padding:2px 5px;
border-radius:4px;font-family:var(--mono);transition:.12s;letter-spacing:.03em}
.link-btn:hover{color:var(--danger);background:color-mix(in srgb,var(--danger) 10%,transparent)}
/* ---------- layout ---------- */
.layout{flex:1;display:grid;grid-template-columns:256px 1fr;min-height:0}
.col{min-height:0;display:flex;flex-direction:column;overflow:hidden}
.col.left{border-right:1px solid var(--line-soft);background:var(--bg-2)}
.col-head{padding:11px 12px 5px;font-size:10px;text-transform:uppercase;letter-spacing:.13em;
color:var(--faint);font-weight:700;display:flex;align-items:center;justify-content:space-between;gap:6px}
.col-head .hint{font-family:var(--mono);letter-spacing:0;text-transform:none;font-size:9px}
.mini-readiness{padding:0 13px 6px;font-family:var(--mono);font-size:8.5px;color:var(--faint);
letter-spacing:.02em;border-bottom:1px solid var(--line-soft);white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.col-body{flex:1;overflow-y:auto;padding:6px 9px 12px}
/* ---------- карточки участников (компактные) ---------- */
.agent-card{background:linear-gradient(180deg,color-mix(in srgb,var(--panel-2) 70%,var(--panel)),var(--panel));
border:1px solid var(--line-soft);border-radius:10px;padding:8px 9px;display:flex;flex-direction:column;gap:6px;
margin-bottom:6px;cursor:pointer;transition:.18s var(--ease);position:relative}
.agent-card:hover{border-color:color-mix(in srgb,var(--faint) 55%,transparent);transform:translateY(-1px);box-shadow:var(--shadow-md)}
.agent-card.active{border-color:color-mix(in srgb,var(--accent) 55%,var(--line));
background:linear-gradient(180deg,color-mix(in srgb,var(--accent) 9%,var(--panel)),var(--panel));
box-shadow:0 0 0 1px color-mix(in srgb,var(--accent) 14%,transparent),0 5px 16px -8px rgba(0,0,0,.6)}
.agent-card.active::before{content:"";position:absolute;left:0;top:9px;bottom:9px;width:3px;
border-radius:3px;background:linear-gradient(180deg,var(--accent-hi),var(--accent));
box-shadow:0 0 9px color-mix(in srgb,var(--accent) 60%,transparent)}
.agent-card.off{opacity:.6}
.agent-card.unavailable{opacity:.5;cursor:not-allowed;background:color-mix(in srgb,var(--danger) 4%,var(--panel))}
.agent-card.unavailable:hover{transform:none;box-shadow:none;border-color:var(--line-soft)}
.ac-top{display:flex;align-items:flex-start;gap:7px}
.ac-check{appearance:none;-webkit-appearance:none;width:14px;height:14px;border:1.5px solid var(--line);
border-radius:4px;background:var(--bg-2);flex:none;position:relative;transition:.15s var(--ease);
margin-top:3px;pointer-events:none}
.ac-check:checked{background:linear-gradient(180deg,var(--accent-hi),var(--accent));border-color:var(--accent)}
.ac-check:checked::after{content:"✓";position:absolute;inset:0;display:grid;place-items:center;
font-size:10px;font-weight:800;color:var(--accent-ink)}
.ac-check:disabled{opacity:.35}
.ac-icon{width:26px;height:26px;border-radius:7px;display:grid;place-items:center;font-weight:700;
font-size:11px;color:#191006;font-family:var(--display);flex:none;
box-shadow:inset 0 -5px 10px rgba(0,0,0,.22),var(--shadow-sm)}
.ac-info{min-width:0}
.ac-name{font-weight:600;font-size:12px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;line-height:1.25}
.ac-role{color:var(--muted);font-size:9.5px;margin-top:1px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.ac-tags{display:flex;gap:3px;flex-wrap:wrap}
.tag{font-family:var(--mono);font-size:8px;font-weight:600;padding:2.5px 5px;border-radius:4px;
letter-spacing:.04em;text-transform:uppercase;line-height:1;border:1px solid transparent;white-space:nowrap}
.t-cdp-ok{background:color-mix(in srgb,var(--ok) 13%,transparent);color:color-mix(in srgb,var(--ok) 80%,#fff);border-color:color-mix(in srgb,var(--ok) 32%,transparent)}
.t-cdp-off{background:color-mix(in srgb,var(--danger) 11%,transparent);color:color-mix(in srgb,var(--danger) 80%,#fff);border-color:color-mix(in srgb,var(--danger) 30%,transparent)}
.t-tab-ok{background:color-mix(in srgb,var(--ok) 8%,transparent);color:color-mix(in srgb,var(--ok) 80%,#fff)}
.t-tab-no{background:color-mix(in srgb,var(--warn) 11%,transparent);color:color-mix(in srgb,var(--warn) 85%,#fff)}
.t-type{background:color-mix(in srgb,var(--panel-3) 80%,transparent);color:var(--muted);border-color:var(--line-soft)}
.t-type-browser{background:color-mix(in srgb,var(--browser) 10%,transparent);color:var(--browser);border-color:color-mix(in srgb,var(--browser) 30%,transparent)}
.t-type-api{background:color-mix(in srgb,var(--api) 9%,transparent);color:var(--api);border-color:color-mix(in srgb,var(--api) 28%,transparent)}
.t-type-codex{background:color-mix(in srgb,var(--codex) 10%,transparent);color:var(--codex);border-color:color-mix(in srgb,var(--codex) 30%,transparent)}
.t-key-ok{background:color-mix(in srgb,var(--ok) 8%,transparent);color:color-mix(in srgb,var(--ok) 80%,#fff)}
.t-key-no{background:color-mix(in srgb,var(--warn) 9%,transparent);color:color-mix(in srgb,var(--warn) 85%,#fff)}
.ac-status{display:flex;justify-content:space-between;align-items:center;gap:6px;
border-top:1px solid var(--line-soft);padding-top:5px;font-size:9px}
.ac-host{font-family:var(--mono);color:var(--faint);font-size:8.5px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.ac-live{display:inline-flex;align-items:center;gap:4px;font-family:var(--mono);font-size:9px;flex:none}
.dot{width:6px;height:6px;border-radius:50%;display:inline-block;flex:none}
.dot.ready{background:var(--ok);box-shadow:0 0 6px color-mix(in srgb,var(--ok) 75%,transparent)}
.dot.think{background:var(--accent);box-shadow:0 0 7px color-mix(in srgb,var(--accent) 80%,transparent);animation:pulse 1s infinite}
.dot.err{background:var(--danger);box-shadow:0 0 6px color-mix(in srgb,var(--danger) 65%,transparent)}
.dot.off{background:var(--faint)}
.lv-ready{color:var(--ok)}.lv-think{color:var(--accent)}.lv-err{color:var(--danger)}.lv-off{color:var(--faint)}
@keyframes pulse{0%,100%{opacity:1}50%{opacity:.3}}
/* ---------- центр: бриф ---------- */
.center{display:flex;flex-direction:column;overflow:hidden;min-width:0}
.briefwrap{padding:13px 16px 10px;border-bottom:1px solid var(--line-soft);background:var(--bg-2)}
.field{margin-bottom:9px}
.field label{display:block;font-size:10px;text-transform:uppercase;letter-spacing:.11em;
color:var(--faint);margin-bottom:5px;font-weight:700}
.field-row{display:flex;gap:10px}
.field-row .field{flex:1}
.field-row .field.fixed{flex:0 0 230px}
.topic-input,.brief{width:100%;background:var(--panel);border:1px solid var(--line);border-radius:10px;
padding:8px 12px;color:var(--ink);resize:vertical;outline:none;transition:.16s var(--ease)}
.topic-input:hover,.brief:hover{border-color:color-mix(in srgb,var(--faint) 60%,transparent)}
.topic-input:focus,.brief:focus{border-color:var(--accent);box-shadow:0 0 0 3px color-mix(in srgb,var(--accent) 16%,transparent)}
select.topic-input{cursor:pointer}
.brief{min-height:70px;font-family:var(--mono);font-size:12px;line-height:1.55}
/* файлы: компактный блок */
.files-mini{display:flex;align-items:center;gap:9px;flex-wrap:wrap}
.files-mini.hover .files-toggle{border-color:var(--accent);color:var(--accent);background:color-mix(in srgb,var(--accent) 8%,transparent)}
.files-toggle{display:inline-flex;gap:6px;align-items:center;background:var(--panel);border:1px solid var(--line-soft);
border-radius:999px;padding:4px 11px;font-size:11px;color:var(--muted);cursor:pointer;transition:.15s var(--ease)}
.files-toggle:hover{color:var(--ink);border-color:color-mix(in srgb,var(--faint) 55%,transparent)}
.files-toggle.open{color:var(--accent);border-color:color-mix(in srgb,var(--accent) 40%,transparent)}
.files-toggle .caret{display:inline-block;transition:transform .15s var(--ease);font-size:9px}
.files-toggle.open .caret{transform:rotate(90deg)}
.files-count{font-family:var(--mono);font-size:10px;color:var(--faint)}
.files-hint{font-size:9.5px;color:var(--faint)}
.files-panel{margin-top:8px}
.drop{border:1.5px dashed var(--line);border-radius:9px;padding:9px;text-align:center;color:var(--muted);
transition:.16s var(--ease);cursor:pointer;font-size:11.5px;background:color-mix(in srgb,var(--panel) 50%,transparent)}
.drop:hover{border-color:color-mix(in srgb,var(--faint) 60%,transparent);color:var(--ink)}
.drop.hover{border-color:var(--accent);color:var(--ink);background:color-mix(in srgb,var(--accent) 7%,transparent);
box-shadow:0 0 0 3px color-mix(in srgb,var(--accent) 10%,transparent)}
.files{margin-top:7px;display:flex;flex-direction:column;gap:5px}
.filechip{display:flex;align-items:center;gap:8px;padding:5px 10px;background:var(--panel);
border:1px solid var(--line-soft);border-radius:var(--r-sm);font-size:12px}
.filechip .meta{color:var(--faint);font-family:var(--mono);font-size:10px}
.filechip .sync{margin-left:auto;font-family:var(--mono);font-size:10px;color:var(--ok)}
.filechip .x{cursor:pointer;color:var(--faint);padding:0 4px;transition:.12s}
.filechip .x:hover{color:var(--danger)}
.actions-bar{display:flex;gap:9px;align-items:center;flex-wrap:wrap;margin-top:2px}
.progress{flex:1;min-width:90px;height:7px;background:var(--line-soft);border-radius:999px;overflow:hidden;display:none}
.progress.show{display:block}
.progress i{display:block;height:100%;width:0;border-radius:999px;
background:linear-gradient(90deg,var(--accent),var(--accent-hi));
box-shadow:0 0 10px color-mix(in srgb,var(--accent) 55%,transparent);transition:width .35s var(--ease)}
/* ---------- автоматизация ---------- */
.autobar{display:flex;align-items:center;gap:13px;padding:8px 16px;background:var(--bg-2);
border-bottom:1px solid var(--line-soft);font-size:12px;flex-wrap:wrap}
.autobar .state{color:var(--muted)}
.autobar .state b{color:var(--ink);font-weight:600}
.autobar .cycle{color:var(--faint);font-family:var(--mono);font-size:10.5px}
/* ---------- табы ---------- */
.tabs{display:flex;gap:4px;padding:9px 16px 0;border-bottom:1px solid var(--line-soft);background:var(--bg-2)}
.tab{position:relative;padding:9px 16px;cursor:pointer;color:var(--muted);font-weight:600;font-size:12.5px;
border-radius:10px 10px 0 0;transition:.15s var(--ease);user-select:none}
.tab:hover{color:var(--ink);background:rgba(255,255,255,.025)}
.tab.active{background:var(--bg);color:var(--ink)}
.tab.active::after{content:"";position:absolute;left:12px;right:12px;bottom:-1px;height:2px;
background:linear-gradient(90deg,var(--accent),var(--accent-hi));border-radius:2px 2px 0 0;
box-shadow:0 0 8px color-mix(in srgb,var(--accent) 60%,transparent)}
.tab .badge-dot{position:absolute;top:3px;right:3px;min-width:15px;height:15px;border-radius:999px;
background:var(--warn);color:#241a02;font-size:9px;font-weight:800;display:none;
align-items:center;justify-content:center;padding:0 4px}
.tab .badge-dot.show{display:flex}
.tabpane{display:none;flex:1;min-height:0;overflow-y:auto}
.tabpane.active{display:flex;flex-direction:column}
/* ---------- план / сетка ответов ---------- */
.preround{margin:14px;padding:15px;background:linear-gradient(180deg,var(--panel-2),var(--panel));
border:1px solid var(--line-soft);border-radius:var(--radius);box-shadow:var(--shadow-sm)}
.preround h3{margin-bottom:9px;font-size:13px;font-family:var(--display);font-weight:600}
.preround .plan{font-size:12px;color:var(--muted);white-space:pre-wrap;max-height:170px;overflow-y:auto;
font-family:var(--mono);line-height:1.6}
.preround .hint{margin-top:10px;color:var(--accent);font-size:11.5px}
.gridwrap{flex:1;overflow-y:auto;padding:14px}
.msgrid{display:grid;grid-template-columns:repeat(auto-fill,minmax(330px,1fr));gap:12px;align-items:start}
.mbox{background:var(--panel);border:1px solid var(--line-soft);border-radius:14px;display:flex;
flex-direction:column;overflow:hidden;transition:.2s var(--ease);box-shadow:var(--shadow-sm)}
.mbox.run{border-color:color-mix(in srgb,var(--accent) 60%,var(--line));
box-shadow:0 0 0 1px color-mix(in srgb,var(--accent) 25%,transparent),0 10px 28px -10px rgba(0,0,0,.65)}
.mbox.err{border-color:color-mix(in srgb,var(--danger) 45%,var(--line))}
.mbox.ok{border-color:color-mix(in srgb,var(--ok) 35%,var(--line))}
.mhead{display:flex;align-items:center;gap:9px;padding:10px 12px;border-bottom:1px solid var(--line-soft);
background:rgba(255,255,255,.018)}
.mava{width:27px;height:27px;border-radius:8px;display:grid;place-items:center;font-weight:700;
font-size:11.5px;color:#191006;font-family:var(--display);flex:none}
.mname{font-weight:600;font-size:12.5px}.mrole{color:var(--muted);font-size:10px}
.mled{margin-left:auto;width:9px;height:9px;border-radius:50%;background:var(--faint);flex:none;transition:.2s}
.mled.run{background:var(--accent);box-shadow:0 0 9px color-mix(in srgb,var(--accent) 80%,transparent);animation:pulse 1.1s infinite}
.mled.ok{background:var(--ok);box-shadow:0 0 9px color-mix(in srgb,var(--ok) 65%,transparent)}
.mled.err{background:var(--danger);box-shadow:0 0 9px color-mix(in srgb,var(--danger) 65%,transparent)}
.mbody{flex:1;padding:11px 12px;overflow-y:auto;font-size:12.5px;line-height:1.6;white-space:pre-wrap;
word-break:break-word;max-height:230px}
.mbody.expanded{max-height:none}
.mbody .sig{margin-top:10px;padding:8px 11px;border-left:3px solid var(--accent);
background:color-mix(in srgb,var(--accent) 8%,transparent);border-radius:0 8px 8px 0;font-size:12px}
.mbody .sig b{color:var(--accent)}
.mbody .err{color:var(--danger)}
.mbody .warnline{color:var(--warn);margin-top:8px;font-size:11px;font-family:var(--mono)}
.mbody .stage{font-size:10.5px;color:var(--muted);margin-top:8px;padding:6px 9px;background:var(--bg);
border-radius:var(--r-xs);border:1px solid var(--line-soft);font-family:var(--mono)}
.mbody .stage .wait{color:var(--warn)}
.mfoot{font-family:var(--mono);font-size:10px;color:var(--faint);padding:7px 12px;
border-top:1px solid var(--line-soft);display:flex;justify-content:space-between;align-items:center;gap:8px}
.expand-btn{cursor:pointer;color:var(--accent);font-size:10px;border:1px solid var(--line);
border-radius:var(--r-xs);padding:2.5px 8px;transition:.12s}
.expand-btn:hover{background:color-mix(in srgb,var(--accent) 10%,transparent);border-color:var(--accent)}
/* ---------- вкладка «Итог»: синтез текстом ---------- */
.summary-text{font-size:13px;line-height:1.7;white-space:pre-wrap;word-break:break-word;color:var(--ink)}
.summary-actions{display:flex;gap:8px;margin-top:14px;flex-wrap:wrap}
.rlink{display:inline-flex;align-items:center;gap:6px;font-family:var(--mono);font-size:10.5px;color:var(--accent);
cursor:pointer;text-decoration:none;border:1px solid color-mix(in srgb,var(--accent) 35%,transparent);
padding:4px 9px;border-radius:7px;background:color-mix(in srgb,var(--accent) 8%,transparent);
transition:.15s var(--ease);margin-top:12px;word-break:break-all}
.rlink:hover{background:color-mix(in srgb,var(--accent) 16%,transparent)}
.log{font-family:var(--mono);font-size:10.5px;color:var(--muted);background:color-mix(in srgb,#000 35%,var(--bg));
border:1px solid var(--line-soft);border-radius:10px;padding:10px;overflow-y:auto;min-height:120px}
.log div{padding:1.5px 0}
.log .t{color:var(--faint)}.log .ok{color:var(--ok)}.log .warn{color:var(--warn)}
/* ---------- drawer ---------- */
.drawer{position:fixed;top:0;right:0;height:100vh;width:min(680px,94vw);background:var(--bg-2);
border-left:1px solid var(--line-soft);box-shadow:var(--shadow-lg);transform:translateX(103%);
transition:transform .3s var(--ease);z-index:60;display:flex;flex-direction:column}
.drawer.show{transform:translateX(0)}
.drawer-head{display:flex;align-items:center;gap:11px;padding:13px 16px;
border-bottom:1px solid var(--line-soft);background:var(--bg)}
.drawer-head h3{margin:0;font-size:14px;flex:1;font-family:var(--display);font-weight:600}
.drawer-body{flex:1;overflow-y:auto;padding:16px 18px}
.drawer .close{font-size:18px;line-height:1;cursor:pointer;color:var(--muted);padding:5px 10px;
border-radius:var(--r-xs);transition:.12s}
.drawer .close:hover{background:var(--panel-2);color:var(--ink)}
.drawer .back{cursor:pointer;border:1px solid var(--line);background:var(--panel);color:var(--ink);
padding:6.5px 12px;border-radius:var(--r-sm);font-size:12px;transition:.15s var(--ease)}
.drawer .back:hover{background:var(--panel-2);border-color:color-mix(in srgb,var(--faint) 55%,transparent)}
.report-body{font-size:13px;line-height:1.65;white-space:pre-wrap;word-break:break-word;
background:var(--bg);border:1px solid var(--line-soft);border-radius:10px;padding:15px;
max-height:calc(100vh - 150px);overflow-y:auto;font-family:var(--mono);font-size:12px}
.history-tools{display:flex;gap:8px;align-items:center;padding:10px 13px;
border-bottom:1px solid var(--line-soft);flex-wrap:wrap}
.history-tools input,.history-tools select{background:var(--panel);border:1px solid var(--line);
border-radius:var(--r-xs);padding:6.5px 10px;color:var(--ink);font-size:11.5px;outline:none}
.history-tools input:focus,.history-tools select:focus{border-color:var(--accent)}
.history{display:flex;flex-direction:column;gap:7px;padding:12px 13px}
.history-item{padding:10px 11px;background:var(--panel);border:1px solid var(--line-soft);
border-radius:var(--r-sm);cursor:pointer;position:relative;transition:.15s var(--ease)}
.history-item:hover{border-color:color-mix(in srgb,var(--faint) 55%,transparent);transform:translateX(3px)}
.history-item b{display:block;font-size:11.5px;padding-right:24px;font-weight:600}
.history-item span{font-size:10px;color:var(--muted);font-family:var(--mono)}
.history-item .del{position:absolute;top:8px;right:8px;cursor:pointer;color:var(--faint);
font-size:13px;line-height:1;transition:.12s}
.history-item .del:hover{color:var(--danger)}
.history-item .ok{color:var(--ok)}.history-item .err{color:var(--danger)}
/* ---------- уведомление ---------- */
.notification{position:fixed;bottom:18px;left:50%;transform:translateX(-50%);z-index:70;
background:var(--panel-2);border:1px solid var(--accent);color:var(--ink);padding:10px 16px;
border-radius:var(--radius);box-shadow:var(--shadow-lg);display:none;align-items:center;gap:12px;
font-size:12.5px;cursor:pointer}
.notification.show{display:flex;animation:rise .25s var(--ease)}
@keyframes rise{from{opacity:0;transform:translateX(-50%) translateY(10px)}to{opacity:1;transform:translateX(-50%) translateY(0)}}
/* ---------- модалка настроек ---------- */
.modal{position:fixed;inset:0;background:rgba(8,6,4,.66);backdrop-filter:blur(4px);
display:none;align-items:center;justify-content:center;z-index:50}
.modal.show{display:flex;animation:fade .18s var(--ease)}
@keyframes fade{from{opacity:0}to{opacity:1}}
.modal-box{width:min(640px,94vw);max-height:90vh;display:flex;flex-direction:column;background:var(--bg-2);
border:1px solid var(--line-soft);border-radius:var(--radius-lg);overflow:hidden;box-shadow:var(--shadow-lg)}
.modal-box h3{padding:17px 20px 5px;font-size:15px;font-family:var(--display);font-weight:600}
.modal-box .sub{color:var(--muted);font-size:12px;padding:0 20px 13px;line-height:1.5}
.sub2{color:var(--muted);font-size:11.5px;margin-bottom:11px;line-height:1.5}
.sub2 b{color:var(--ink);font-family:var(--mono);font-size:10.5px}
.modal-tabs{display:flex;gap:4px;padding:0 16px;border-bottom:1px solid var(--line-soft);flex-wrap:wrap}
.modal-tab{padding:9px 13px;cursor:pointer;color:var(--muted);font-weight:600;font-size:12px;
border-bottom:2px solid transparent;transition:.15s var(--ease);user-select:none}
.modal-tab:hover{color:var(--ink)}
.modal-tab.active{color:var(--ink);border-bottom-color:var(--accent)}
.modal-body{flex:1;overflow-y:auto;padding:14px 20px}
.opt{display:flex;align-items:center;justify-content:space-between;gap:12px;padding:11px 0;
border-top:1px solid var(--line-soft)}
.opt:first-child{border-top:none}
.opt label{font-weight:500}
.opt .hint{margin-top:2px}
.opt input[type=number]{width:84px;background:var(--panel);border:1px solid var(--line);
border-radius:var(--r-xs);padding:7px 9px;color:var(--ink);outline:none}
.opt input[type=text],.opt input[type=password]{width:280px;background:var(--panel);border:1px solid var(--line);
border-radius:var(--r-xs);padding:7px 9px;color:var(--ink);font-family:var(--mono);font-size:11px;outline:none}
.opt input:focus{border-color:var(--accent);box-shadow:0 0 0 3px color-mix(in srgb,var(--accent) 16%,transparent)}
.toggle{width:38px;height:21px;border-radius:999px;background:var(--line);position:relative;
cursor:pointer;transition:.2s var(--ease);flex:none}
.toggle::after{content:"";position:absolute;top:2px;left:2px;width:17px;height:17px;border-radius:50%;
background:var(--muted);transition:.2s var(--ease);box-shadow:var(--shadow-sm)}
.toggle.on{background:color-mix(in srgb,var(--accent) 55%,transparent)}
.toggle.on::after{left:19px;background:var(--accent-hi)}
.gate{display:flex;align-items:center;gap:10px;padding:9px 11px;background:var(--panel);
border:1px solid var(--line-soft);border-radius:var(--r-sm);margin:7px 0}
.gate .gled{width:8px;height:8px;border-radius:50%;flex:none}
.gate .gled.ok{background:var(--ok);box-shadow:0 0 7px color-mix(in srgb,var(--ok) 75%,transparent)}
.gate .gled.warn{background:var(--warn);box-shadow:0 0 7px color-mix(in srgb,var(--warn) 75%,transparent)}
.gate .gtext{font-size:11.5px}
.gate .gtext b{display:block;font-weight:600}
.gate .gtext span{color:var(--muted);font-size:10.5px}
.ep{margin:10px 0;padding:12px;background:var(--panel);border:1px solid var(--line-soft);border-radius:10px}
.ep .ept{font-weight:600;margin-bottom:9px;display:flex;justify-content:space-between;align-items:center}
.ep .ept .set{font-family:var(--mono);font-size:10px;color:var(--ok)}
.ep .epr{display:flex;gap:8px;align-items:center;margin-top:7px;flex-wrap:wrap}
.ep .epr label{font-size:11px;color:var(--muted);flex:0 0 62px}
.ep .epr input{flex:1;min-width:200px}
.modal-actions{display:flex;gap:9px;justify-content:flex-end;padding:13px 20px;
border-top:1px solid var(--line-soft);background:var(--bg-2)}
/* ---------- список тем ---------- */
.theme-list{display:flex;flex-direction:column;gap:7px;margin-bottom:12px}
.theme-card{display:flex;align-items:center;gap:11px;padding:9px 12px;background:var(--panel);
border:1px solid var(--line-soft);border-radius:10px;cursor:pointer;transition:.15s var(--ease);text-align:left;width:100%}
.theme-card:hover{border-color:color-mix(in srgb,var(--faint) 55%,transparent);transform:translateX(2px)}
.theme-card.active{border-color:var(--accent);box-shadow:0 0 0 1px color-mix(in srgb,var(--accent) 25%,transparent)}
.theme-card .sw{width:26px;height:26px;border-radius:7px;flex:none;border:1px solid var(--line)}
.theme-card .tt b{font-size:12px;display:block}
.theme-card .tt span{font-size:10px;color:var(--muted)}
.theme-card .chk{margin-left:auto;color:var(--accent);font-weight:700}
/* ---------- адаптив ---------- /
.menu-btn{display:none}
@media(max-width:1080px){
.layout{grid-template-columns:1fr}
.col.left{position:fixed;top:0;left:0;height:100vh;width:264px;z-index:55;transform:translateX(-103%);
transition:transform .25s var(--ease);box-shadow:var(--shadow-lg)}
.col.left.show{transform:translateX(0)}
.menu-btn{display:inline-flex}
.field-row{flex-direction:column;gap:0}
.field-row .field.fixed{flex:1}
}
@media(prefers-reduced-motion:reduce){{transition:none!important;animation:none!important}}