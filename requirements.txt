# Корневой requirements — единый набор через constraints.txt (Директива §6 Q-09).
# Все версии зафиксированы в constraints.txt; здесь только ссылка, чтобы
# корневой и uni/requirements.txt НЕ расходились.
-r constraints.txt
