# UNI unified dependency constraints (Директива §6 Q-09).
#
# Единый источник истины для зависимостей. Корневой requirements.txt и
# uni/requirements.txt ссылаются на этот файл через `-r constraints.txt`,
# чтобы версии НЕ расходились (раньше были две копии с разными диапозонами,
# напр. torch>=2.0 в root против torch>=2.6 в uni).
#
# Для жёсткой фиксации (deterministic installs) рекомендуется сгенерировать
# locked-файл через pip-compile (не обязательно для запуска):
#     pip install pip-tools
#     pip-compile constraints.txt -o constraints.lock.txt
# и затем `-r constraints.lock.txt` вместо `-r constraints.txt`.
#
# Диапазоны подобраны по фактическому графу импортов репозитория (A-09):
# проверено `import uni` + запуск pytest. winrt-Windows.Media.Ocr (V-04) и
# uiautomation (Tier-0) НЕ включены сюда — ставятся опционально на целевой
# Windows-машине (отсутствие не ломает import благодаря ленивым импортам).

pydantic>=2.6
pydantic-settings>=2.1
pyyaml>=6.0
openai>=1.30
playwright>=1.40
pyautogui>=0.9
comtypes>=1.2
opencv-python>=4.10,<5
faster-whisper>=1.1
piper-tts>=1.6
sounddevice>=0.5
soundfile>=0.12
pillow>=10.0
rich>=13.0
gradio_client>=1.3
torch>=2.6
numpy>=1.26
mss>=9.0
buttplug-py>=0.2.0
